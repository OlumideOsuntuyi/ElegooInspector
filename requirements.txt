# Required
PyQt6>=6.5
opencv-contrib-python>=4.8
numpy>=1.24

# Optional; each unlocks one feature and the app degrades gracefully without it
pygame>=2.5          # gamepad / joystick support
mediapipe>=0.10      # finger-pose control
ultralytics>=8.0     # YOLOv8 object detection (falls back to SSD, then faces)
